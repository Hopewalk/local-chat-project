# Copy this file to '.env' and edit it with your settings.

# MongoDB Connection Configuration
# If MongoDB is on another PC, change localhost to that PC's IP (e.g. 192.168.1.15)
MONGO_URI=mongodb://localhost:27017/
MONGO_DB=lumina_chat

# Flask Server Configuration
FLASK_PORT=5001
FLASK_DEBUG=True
